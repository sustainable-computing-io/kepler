extends: default
rules:
  line-length: disable
  document-start: disable
  comments:
    min-spaces-from-content: 1
  quoted-strings:
    required: only-when-needed
    extra-required:
      - ^.*:\s.*$
      - ^.*:$
    quote-type: double
